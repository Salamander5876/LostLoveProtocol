# LLP Client для Windows (C#)

Современный VPN клиент на C# для Windows без зависимости от Visual Studio Build Tools!

## Преимущества

✅ **Не нужен Visual Studio** - только .NET SDK
✅ **Быстрая установка** - 2 минуты вместо 15
✅ **Красивый интерфейс** - цветная консоль с Spectre.Console
✅ **Один EXE файл** - самодостаточный бинарник
✅ **Нативная производительность** - AOT компиляция

## Быстрый старт

### 1. Установите .NET 8 SDK

**Способ 1: winget (рекомендуется)**
```powershell
winget install Microsoft.DotNet.SDK.8
```

**Способ 2: Скачать вручную**
- Перейдите: https://dotnet.microsoft.com/download/dotnet/8.0
- Скачайте ".NET 8.0 SDK" (x64)
- Установите (~200 MB, 1-2 минуты)

### 2. Перезапустите PowerShell

**Важно!** Закройте и откройте PowerShell заново после установки .NET

### 3. Соберите клиент

```powershell
cd C:\LostLoveProtocol\client-csharp\LLPClient
dotnet publish -c Release -r win-x64 --self-contained

# Бинарник будет в:
# bin\Release\net8.0\win-x64\publish\LLPClient.exe
```

### 4. Скопируйте в удобное место

```powershell
# Создайте папку
New-Item -ItemType Directory -Force -Path C:\LLP

# Скопируйте клиент
Copy-Item bin\Release\net8.0\win-x64\publish\LLPClient.exe C:\LLP\

# Создайте папку для конфигов
New-Item -ItemType Directory -Force -Path C:\LLP\configs
```

### 5. Запустите

```powershell
cd C:\LLP
.\LLPClient.exe
```

Или с конфигурацией напрямую:

```powershell
.\LLPClient.exe --config configs\client1.toml
```

## Получение конфигурации с сервера

### На VPS:

```bash
cd ~/LostLoveProtocol
./target/release/llp-server --export-client-config client1.toml
```

### Скачайте на Windows:

**Способ 1 - SCP:**
```powershell
scp root@your-vps-ip:~/LostLoveProtocol/client1.toml C:\LLP\configs\
```

**Способ 2 - WinSCP:**
1. Установите WinSCP: https://winscp.net
2. Подключитесь к VPS
3. Скачайте `client1.toml` в `C:\LLP\configs\`

## Использование

### Интерактивное меню

Просто запустите без параметров:

```powershell
cd C:\LLP
.\LLPClient.exe
```

Вы увидите красивое меню со списком конфигураций:

```
 _     _     ____     ____ _ _            _
| |   | |   |  _ \   / ___| (_) ___ _ __ | |_
| |   | |   | |_) | | |   | | |/ _ \ '_ \| __|
| |___| |___|  __/  | |___| | |  __/ | | | |_
|_____|_____|_|      \____|_|_|\___|_| |_|\__|

LostLoveProtocol VPN Client v1.0.0
Windows .NET Implementation

╭────────┬─────────────────────────┬──────────╮
│   #    │     Конфигурация        │  Размер  │
├────────┼─────────────────────────┼──────────┤
│   1    │ client1                 │  0.85 KB │
│   2    │ work-vpn                │  0.92 KB │
╰────────┴─────────────────────────┴──────────╯

[R] Обновить  [Q] Выход

Выберите конфигурацию: _
```

### Прямое подключение

```powershell
.\LLPClient.exe --config configs\client1.toml
```

### Отключение

Нажмите `Ctrl+C` для отключения

## Статистика в реальном времени

Во время работы вы увидите статистику:

```
╭─────────────────────────────────────╮
│  Подключение к VPN                  │
│                                     │
│  Сервер: 123.45.67.89:8443         │
│  VPN IP: 10.8.0.2                  │
│  Профиль: vk_video                 │
╰─────────────────────────────────────╯

✓ Подключено!

↑ Отправлено    1.23 MB (1547 пакетов)
↓ Получено      5.67 MB (4892 пакета)
⏱ Время работы  00:05:32
📡 Сервер       123.45.67.89:8443
```

## Проверка подключения

Откройте новое окно PowerShell:

```powershell
# Проверка интерфейса
ipconfig

# Ping сервера через VPN
ping 10.8.0.1

# Проверка маршрутизации
tracert 8.8.8.8
```

## Структура конфигурации

Пример `client1.toml`:

```toml
[server]
address = "123.45.67.89:8443"

[vpn]
interface_name = "llp0"
ip_address = "10.8.0.2"
subnet_mask = "255.255.255.0"
mtu = 1420

[security]
mimicry_profile = "vk_video"
enable_replay_protection = true
max_packet_age_sec = 60

[reconnect]
enable = true
initial_delay_ms = 1000
max_delay_ms = 30000
max_attempts = 0  # 0 = infinite

[logging]
level = "info"
```

## Разработка

### Требования для разработки

- .NET 8 SDK
- Visual Studio 2022 или VS Code (опционально)
- C# 12+

### Сборка в режиме отладки

```powershell
cd client-csharp\LLPClient
dotnet build
```

### Запуск из исходников

```powershell
dotnet run -- --config configs\client1.toml
```

### Добавление зависимостей

```powershell
dotnet add package PackageName
```

## Roadmap

### v1.0 (MVP) - Текущая версия
- ✅ Красивый консольный интерфейс
- ✅ Чтение TOML конфигурации
- ✅ TCP подключение к серверу
- ⏳ TUN device (заглушка, требует Wintun)
- ⏳ Криптография (заглушка)
- ⏳ Мимикрия (заглушка)

### v1.1 - Полная функциональность
- 🔲 Wintun интеграция для TUN интерфейса
- 🔲 X25519 + ChaCha20-Poly1305 криптография
- 🔲 VK Video / Yandex Music / RuTube мимикрия
- 🔲 Handshake протокол
- 🔲 Переподключение при обрыве
- 🔲 Keepalive

### v1.2 - Улучшения
- 🔲 GUI версия (WPF/Avalonia)
- 🔲 Системный трей
- 🔲 Автозапуск
- 🔲 Метрики и логирование

## Устранение проблем

### Ошибка ".NET SDK не найден"

```powershell
# Проверка установки
dotnet --version

# Должно показать: 8.0.xxx
```

Если не показывает - переустановите .NET SDK

### Ошибка "Требуются права администратора"

Запустите PowerShell **от имени администратора**

### Клиент не создаёт TUN интерфейс

Текущая версия использует заглушку для TUN. Полная реализация через Wintun будет в v1.1.

## Сравнение с Rust версией

| Характеристика | Rust Client | C# Client |
|----------------|-------------|-----------|
| Установка компилятора | 15 минут + 6 GB | 2 минуты + 200 MB |
| Требует Visual Studio | ✅ Да | ❌ Нет |
| Время компиляции | 5-10 минут | 30 секунд |
| Размер бинарника | ~5 MB | ~20 MB |
| Производительность | Отличная | Очень хорошая |
| Разработка | Сложнее | Проще |

## Лицензия

Смотрите LICENSE в корне репозитория

---

**Вопросы?** Создайте issue на GitHub!
