# Запуск LLP Client на Windows

Пошаговая инструкция для запуска VPN клиента на Windows.

## Требования

- Windows 10/11
- Права администратора
- Интернет для установки Rust (если не установлен)

## Быстрый старт

### 1. Откройте PowerShell от имени администратора

**Способ 1:**
1. Нажмите `Win + X`
2. Выберите "Windows PowerShell (Администратор)" или "Terminal (Admin)"

**Способ 2:**
1. Нажмите `Win + S` (поиск)
2. Введите `PowerShell`
3. Правый клик → "Запуск от имени администратора"

### 2. Перейдите в папку клиента

```powershell
cd C:\LostLoveProtocol\client
```

### 3. Разрешите выполнение скриптов (если требуется)

Если при запуске скрипта появляется ошибка "execution policy", выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 4. Запустите клиент

```powershell
.\LLP-Client.ps1
```

## Что произойдет при первом запуске

Скрипт автоматически:

1. ✅ **Проверит Rust**
   - Если не установлен → предложит установить
   - Загрузит rustup-init.exe (~10 MB)
   - Установит Rust (~500 MB)

2. ✅ **Скомпилирует клиент**
   - Запустит `cargo build --release -p llp-client`
   - Займет 2-5 минут
   - Создаст `llp-client.exe`

3. ✅ **Создаст папку configs**
   - `C:\LostLoveProtocol\client\configs\`

## Получение конфигурации с сервера

### Способ 1: SCP (командная строка)

```powershell
# На Windows (в обычном PowerShell, не от админа)
scp root@your-vps-ip:~/LostLoveProtocol/client1.toml C:\LostLoveProtocol\client\configs\
```

### Способ 2: WinSCP / FileZilla

1. Скачайте [WinSCP](https://winscp.net) или [FileZilla](https://filezilla-project.org)
2. Подключитесь к VPS (Host: ваш IP, Username: root)
3. Скачайте файл `~/LostLoveProtocol/client1.toml`
4. Сохраните в `C:\LostLoveProtocol\client\configs\`

### Способ 3: Копирование содержимого

**На VPS:**
```bash
cat ~/LostLoveProtocol/client1.toml
```

**На Windows:**
1. Скопируйте вывод
2. Создайте файл `C:\LostLoveProtocol\client\configs\client1.toml`
3. Вставьте содержимое

## Использование клиента

### Главное меню

После запуска вы увидите меню:

```
╔════════════════════════════════════════════════════════════╗
║                  ДОСТУПНЫЕ КОНФИГУРАЦИИ                    ║
╠════════════════════════════════════════════════════════════╣
║ [1] client1                                      0.85 KB ║
║ [2] work-vpn                                     0.92 KB ║
╠════════════════════════════════════════════════════════════╣
║ [R] Обновить список  [Q] Выход                             ║
╚════════════════════════════════════════════════════════════╝
```

### Подключение

1. Введите номер конфигурации (например, `1`)
2. Клиент подключится к серверу
3. Для отключения нажмите `Ctrl+C`

### Горячие клавиши

- `R` - Обновить список конфигураций
- `Q` - Выход
- `Ctrl+C` - Отключиться от VPN

## Проверка подключения

После подключения откройте **новое** окно PowerShell и выполните:

```powershell
# Проверка сетевого интерфейса
ipconfig

# Должен появиться интерфейс llp0 с IP 10.8.0.x

# Ping сервера через VPN
ping 10.8.0.1

# Проверка маршрута
tracert 8.8.8.8
```

## Структура конфигурационного файла

Пример `client1.toml`:

```toml
[server]
address = "123.45.67.89:8443"  # IP вашего VPS

[vpn]
interface_name = "llp0"
ip_address = "10.8.0.2"        # Уникальный для каждого клиента
subnet_mask = "255.255.255.0"
mtu = 1420

[security]
mimicry_profile = "vk_video"   # Должен совпадать с сервером
enable_replay_protection = true
max_packet_age_sec = 60

[reconnect]
enable = true
initial_delay_ms = 1000
max_delay_ms = 30000
max_attempts = 0  # 0 = бесконечно

[logging]
level = "info"
```

## Несколько клиентов

Каждый клиент должен иметь **уникальный IP**:

- Клиент 1: `ip_address = "10.8.0.2"`
- Клиент 2: `ip_address = "10.8.0.3"`
- Клиент 3: `ip_address = "10.8.0.4"`

Создайте отдельные конфигурации для каждого устройства.

## Устранение проблем

### Ошибка "execution policy"

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка "Требуются права администратора"

Запустите PowerShell от имени администратора (см. Шаг 1).

### Rust не устанавливается

1. Скачайте вручную: https://rustup.rs/
2. Запустите `rustup-init.exe`
3. Перезапустите PowerShell

### Ошибка компиляции

```powershell
# Перейдите в корень проекта
cd C:\LostLoveProtocol

# Исправьте ошибки компиляции
bash fix-compilation.sh  # Если есть Git Bash

# Или скомпилируйте вручную
cargo build --release -p llp-client
```

### Клиент не подключается

1. **Проверьте IP сервера** в конфигурации
   ```powershell
   notepad C:\LostLoveProtocol\client\configs\client1.toml
   ```

2. **Проверьте доступность сервера**
   ```powershell
   Test-NetConnection -ComputerName your-vps-ip -Port 8443
   ```

3. **Проверьте файрвол на сервере**
   ```bash
   # На VPS
   ufw status | grep 8443
   systemctl status llp-server
   journalctl -u llp-server -n 50
   ```

### TUN интерфейс не создается

Это известная проблема на Windows. LLP использует специальную библиотеку для работы с TUN интерфейсами.

**Решение:**
1. Убедитесь, что PowerShell запущен от администратора
2. Проверьте логи клиента на наличие ошибок
3. Попробуйте перезагрузить Windows

## Автозапуск клиента

Для автоматического подключения при загрузке Windows:

1. Нажмите `Win + R`
2. Введите `shell:startup`
3. Создайте ярлык на `LLP-Client.ps1`
4. В свойствах ярлыка добавьте:
   - Целевой объект: `powershell.exe -ExecutionPolicy Bypass -File "C:\LostLoveProtocol\client\LLP-Client.ps1"`
   - Запуск: От имени администратора

## Логи

Логи клиента выводятся в консоль. Для сохранения в файл:

```powershell
.\LLP-Client.ps1 | Tee-Object -FilePath "llp-client.log"
```

## Обновление клиента

```powershell
cd C:\LostLoveProtocol
git pull origin main
bash fix-compilation.sh  # Если нужно

# Пересобрать клиент
cd client
.\LLP-Client.ps1
# Выберите пересборку при запросе
```

## Производительность

- **CPU**: ~1-5% при активном использовании
- **RAM**: ~10-30 MB
- **Трафик**: Зависит от профиля мимикрии и нагрузки

---

## Быстрая шпаргалка

```powershell
# 1. Открыть PowerShell (Администратор)
Win + X → Windows PowerShell (Администратор)

# 2. Перейти в папку
cd C:\LostLoveProtocol\client

# 3. Разрешить скрипты (однократно)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 4. Запустить клиент
.\LLP-Client.ps1

# 5. Выбрать конфигурацию → Подключиться
# 6. Ctrl+C для отключения
```

Готово! Вы подключены к VPN! 🚀
